services:
  db:
    image: postgres:16
    container_name: wallet-db
    restart: always
    environment:
      POSTGRES_DB: wallet
      POSTGRES_USER: wallet_user
      POSTGRES_PASSWORD: wallet_pass
    ports:
      - "5432:5432"
